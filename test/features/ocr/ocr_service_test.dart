import 'package:flutter_test/flutter_test.dart';

// TODO: import OcrService, MlKitOcrService, Result

/// Тесты для OCR-модуля (§5.1 ТЗ, сценарий «Прочитай»).
///
/// Покрываемые случаи:
/// - Успешное распознавание печатного текста
/// - Пустой результат → Failure с сообщением «Текст не обнаружен»
/// - Низкий confidence → Failure с советом поднести ближе
/// - Невалидное изображение → корректная обработка ошибки
void main() {
  group('OcrService', () {
    test('распознаёт печатный текст с высоким confidence', () {
      // Arrange — подготовить тестовое изображение с текстом
      // Act — вызвать recognizeText()
      // Assert — Success с непустым текстом
      // TODO: Реализовать
    });

    test('возвращает Failure при отсутствии текста', () {
      // Arrange — пустое/однородное изображение
      // Assert — Failure(OCR_NO_TEXT, ...)
      // TODO: Реализовать
    });

    test('возвращает блоки текста в порядке слева→направо, сверху→вниз', () {
      // TODO: Проверить порядок блоков
    });
  });
}
